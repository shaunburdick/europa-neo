# AGENTS.md — Working Charter for AI Agents

This file governs any AI agent (primary or subagent) doing work in this repository. Read it fully before acting. It exists so that any agent — in any fresh session — can resume this project without re-deriving context.

## Project vision

**Europa Neo**: a modern TypeScript/Node reimplementation of *Europa*, a 1990s Java-applet real-time multiplayer war game (nanobot warfare on Jupiter's moon Europa). Goal: open-source and self-hostable. The original's features are the inspiration; the core gameplay loop is preserved faithfully while UX, controls, and visuals are modernized. This is **not** a pixel-faithful port.

## Binding product decisions (do not relitigate)

1. **Fidelity**: keep the core loop (cities / pipes / fog-of-war) faithful; modernize everything else; QoL features welcome.
2. **V1 scope**: gameplay first — matchmaking → battle → victory. Accounts, ratings ladder, and chat are future features.
3. **Frontend**: free rein within TypeScript; specs describe capabilities, never specific rendering libraries.
4. **Matches**: two visibility types — public (lobby-listed) and private (joinable only via generated ID/shareable link; never lobby-listed).
5. Engine supports 2–4 players by contract; v1 ships 2-player end-to-end.
6. **Distribution**: self-hosted only; npm packages stay private (`private: true` everywhere) and are never published to any registry.

## Governing documents (read in this order)

| Document                                        | Role                                                        |
| ----------------------------------------------- | ----------------------------------------------------------- |
| `AGENTS.md` (this file)                          | Agent working rules + project state                         |
| `.specify/memory/constitution.md`                | Non-negotiable engineering principles                       |
| `specs/001…007/spec.md`                         | Feature specifications (the source of truth for behavior)   |
| `europa-source/games.dangerous-minds.net/Europa/html/Europa/rules.html` | Original mechanics — authoritative when specs are ambiguous |
| `europa-source/.../controls.html`                | Original control scheme (client console must match its capabilities) |

### Constitution summary (full text in `.specify/memory/constitution.md`)

TypeScript strict mode · server-authoritative deterministic tick simulation · ≥80% test coverage on game logic (merge gate) · specs-as-documentation · simplicity over cleverness · accessibility-minded UI · self-hostable by default.

## Current state (update this section as work progresses!)

- **Branch**: `main` post-MVP merge (PR #1 merged as `b1243cb`, 2026-08-25) — all new work on feature branches off `main`; never commit to `main` directly
- **Completed**:
  - **Specs layout migration (issue #12)** (branch `issue-12-specs-migration`, 2026-08-25) — legacy feature dirs 001–007 `git mv`-ed from the legacy dot-specify features layout to root `specs/` (64 files, history preserved), joining 008–010; the empty legacy dir was removed. All old-path references updated repo-wide (102 tracked paths touched): AGENTS.md governing-documents table + README.md, six CI workflows' path filters (`specs/NNN-*/**`), package READMEs + src doc comments, six contract-drift/conformance test suites that READ spec contracts at runtime (engine/fog/terrain/networking/console — all re-run green against new paths), `biome.jsonc` contract exclusion now `!specs/*/contracts/**`, historical planning docs incl. specs/009 and `.specify/pm-handoff.md` (PM ruling: zero old-layout references remain in tracked files — grepping tracked files for the old dot-specify features path is the standing regression check). Spec-kit helper scripts verified location-agnostic (`SPECIFY_FEATURE_DIRECTORY`/`feature.json`-driven): check-prerequisites/setup-plan/setup-tasks dry-runs resolve migrated features; latest-feature detection (`sort -V` over `specs/`) still picks 010. Untracked stale copies remain only in gitignored build output (`dist/`, `coverage/`) — regenerated on next build.
  - **007 player manual: Implemented** (2026-08-24, waves T001→T020) — 13 plain-Markdown pages under `docs/manual/` (index + quick-start, objective, the-board, cities-and-troops, pipes, combat, special-weapons, reserves, fog-of-war, controls, reading-the-screen, numbers), every control row audited both directions against `DEFAULT_INPUT_MAPPING` + HUD components (SC-001 zero drift) and every number traced to its constant declaration line (SC-002 zero drift; incl. `flowUphillFactor: 0`, paratroop 20→10, gun friendly fire, reserves 0–90%, grace 60 s). `.github/workflows/pages-deploy.yml` = official chain checkout v4.2.2 → configure-pages v6.0.0 → jekyll-build-pages v1.0.13 → upload-pages-artifact **v5.0.0** (re-verified at implementation per plan decision 4 — newer than the plan's planning-time v3.x note; pairs with deploy-pages v5.0.0) → deploy-pages v5.0.0, all SHA-pinned with version comments; push to `main` path-gated on `docs/manual/**` + the workflow file + `workflow_dispatch`; `contents: read` top-level, deploy job elevates to `pages: write` + `id-token: write` on the `github-pages` environment; concurrency group `pages`; FR-016 header documents the one-time Settings → Pages → Source = "GitHub Actions" prerequisite and the visible-failure remedy. Structural validation 20/20 (YAML parse, trigger/permission/artifact-scope conformance); live deploy proof is SC-004, post-merge by design (main-only trigger). README gained a gameplay-only "Player manual" section (Pages URL + raw Markdown link + fork-owner enablement note). PM-notable: shipped console status union has EIGHT values — spec FR-009's parenthetical listed seven; manual documents all eight (`closed` included) per FR-009's operative "every console status value" wording. Spec status flipped to Implemented.
  - Spec 007 player manual (`docs(spec)` earlier session) — end-user instruction book + GitHub Pages publishing spec written at `specs/007-player-manual/spec.md`; folded into PR #1 by product-owner decision (no `007-*` branch). Plan/research/tasks committed at `8502349`.
  - Phases 1–3 — constitution ratified; six feature specs written and committed (`859a4f3`); spec 006 amended for visibility types + shareable links (`1ed3233`); spec 006 stamped v1.1 with inline Clarifications trail (`b55bfaf`); original archive trimmed to documentation subset with history purged (`79701f8`+)
  - Phases 4–5 — all six feature plans + tasks committed
  - **001 engine: Implemented** (commits `d18f31c` → `4f60216`) — full US1-US5 + Polish: 280 tests passing, ≥80% coverage on every metric, byte-identical 10k-tick determinism (SC-001), median tick < 0.1ms on 32×32 board (SC-004), 3/4-player smoke (FR-019), CI workflow + README + contract-drift detector in place. All Wave 1 reviewer items addressed.
  - **003 terrain: Implemented** (commits `30f3e11` → `3597bae`) — full US1-US3 + Polish: 225 tests passing, ≥80% coverage on every metric (93.17% statements / 83.28% branches / 94.11% functions / 95.31% lines), byte-identical 1k-seed determinism (SC-001), p99 < 1s for default map (SC-003), 200-map balance suite (SC-002/SC-004), all 9 FRs + all 4 SCs green. PM-approved additive change: `effectiveSettings` field added to `MapStats` and `TerrainGenerationResult` for FR-008 caller visibility. CI workflow + README + contract-drift detector in place. Spec status flipped to Implemented.
  - **002 fog: Implemented** (Wave 5A `546747a` + Wave 5B) — full US1-US3 + Polish: 107 tests passing, ≥80% coverage on every metric (100% statements / 94.25% branches / 100% functions / 100% lines), Chebyshev horizon + structural redaction + spectator mode, byte-identical 100-trial view hashes (SC-001 micro), zero-leakage 500-tick scripted-match audit vs independent oracle (SC-001 protocol), p99 < 1ms on 32×32 best-of-3 rounds (SC-004), engine-conformance + contract-drift detectors, CI workflow + README in place. PM-notable additive change: optional `events` field added to `ComputePlayerViewOptions` (spec + local copy updated in same change set) because the engine's `World` carries no events — they arrive via `tick()`'s `TickResult`. Spec status flipped to Implemented.
  - **004 networking: Implemented** (US1-US3 through Wave 6B; Polish Wave 6C-2 `1595faf` → `be9291e`; review remediation Wave 6C-3 `8f8a9aa` → `09f887e`) — full FR-001..FR-011 + SC-001..SC-005: 184 tests passing across 27 files, ≥80% coverage on every metric (94.23% statements / 84.88% branches / 97.77% functions / 94.32% lines), byte-identical scripted-match determinism (SC-001), sustained-cadence soak at production 250 ms (SC-005 per Clarifications v1.1: 38 contiguous ticks, median < 15 ms budget, generous p99 guard), wire-level version-policy + rate-limit + conformance tests (byte-identity of contract mirrors, union exhaustiveness), README + network-ci.yml in place (green on first run). Code-quality-reviewer checkpoint completed: HOLD verdict items all remediated (maxPayload frame cap + ws error listener, idle-client staleness sweep enforcing FR-009 first clause, lookup-before-consume reconnect token validation, contract doc fixes incl. `token_mismatch` union addition, unknown-role rejection, resync cursor hardening). PM-notable changes: pre-1.0 minor = breaking boundary for FR-004 (Wave 6B-1 ruling); SC-005 re-scoped from timed concurrency soak to cadence-stability protocol (spec Clarifications v1.1); SnapshotPayload `{world}` → `{tick, view}` fog-leak fix (specs-stay-truthful, same change set). Spec status flipped to Implemented; quickstart.md gained a validation-results appendix mapping Q-N01..Q-N10 to real suites.
  - **006 matchmaking: Implemented** (Waves 7A–7E; Phase 8 Polish `ba8458e` → `e24fed9`) — full US1-US5 + Polish: 167 tests across 33 files, ≥80% coverage on every metric (94.69% statements / 84.90% branches / 94.56% functions / 94.59% lines), quickstart Q-M01..Q-M08 all green, SC-005 soak (50 create/join/terminal/rematch cycles → zero active matches + zero active sessions), upstream-shape conformance suite, README + matchmaking-ci.yml in place. PM-notable changes (spec Implementation Notes): double-request rematch idempotency ruling; additive `MatchRecord.initialSeed` for rematch pre-minted seeds; lazy read-path GC sweeps with no timers (`sweepIntervalMs` = host hint) — empty-match TTL sweep collects unstarted filling matches and deletes their sessions; conformance-clause rewrite (engine never shipped `createMatchSession`; shipped `DetachRequest` has no `reason` field); filling-forfeit inline release instead of full leaveMatch semantics.
  - **005 console: Implemented** (Waves 8A–8F; Phase 8 Polish `83e3d51` → `2d237ba`) — full US1-US5 + Polish T086-T097: `ConsoleRuntime` + `createConsole` factory + final public barrel; conformance suite (byte-identity ×4 mirrors, upstream type mutual-assignability, dist surface completeness via compile-time witness table + strict `typecheck:conformance` program); SC-002 determinism vs committed golden fixture (`tests/fixtures/golden-1000-tick.json`, generator script included); SC-003 perf budgets in real Chromium with self-calibrating batch timing (paint p50 1.8ms < 8ms, reduce ~0.2µs < 1ms, preflight ~0.3µs < 0.1ms); selfhost E2E + gzip bundle budget over browser payload (76,528 bytes < 150KB); merged node+browser projects coverage ≥80% on every metric (88.74% statements / 81.28% branches / 88.65% functions / 88.66% lines); console suites: unit 192 · component 25 · a11y 19 · e2e 7 · perf 3 · determinism 3 · parity 2 · conformance 9 (= 260). PM-notable changes (spec Implementation Notes): no rAF paint loop — React commits drive painting; scheduleReconnect/showErrorModal/playSound effects handled defensively (never emitted in v1); bundle budget scoped to `dist/assets`; conformance typecheck program added because tests are excluded from every package tsconfig repo-wide; `allocateActionId()` exported so host wire orders share the gesture-id counter; built-in surrender modal triggered via `surrenderRequestEpoch` prop; fresh-clone CI simulation passed (node_modules + dist wiped, all steps replayed in order). Spec status flipped to Implemented; quickstart.md gained a validation mapping appendix.
  - **Integration wave: DONE** — full production path proven end-to-end: semantic console UI (`/lobby` and `/match/<id>` routes) ⇄ real browser WebSocket client ⇄ real `createMatchServer` (ephemeral port) ⇄ matchmaking-bound engine+terrain+fog. The production bootstrap mounts the lobby runtime for the canonical lobby and semantic match paths; it resolves match entry through the authoritative lobby snapshot before attaching the player or spectator leg.
    - `@europa/networking/browser` subpath export (browser-safe wire codec: frame encode/decode + version/validator + errors; no `ws`/`node:*`) so client and server share one codec.
    - `packages/console/src/net/ws-match-client.ts`: first runtime implementation of feature 004's contract-only `MatchClient` over native WebSocket — hello/join handshake, order seq stamping + ack correlation, heartbeat pings at half the server-advertised interval (server idle-sweeps silent connections at `wsIdleTimeoutMs`), reconnect-token joins completed by snapshot, transport-loss → `disconnected` transitions. Now the DEFAULT `matchClientFactory` for `createConsoleClient` (the documented evolution of the T029 adapter); hosts may still inject.
    - `src/internal/lobby-runtime.tsx` is the production mount for `/lobby`, `/match/<id>`, `/match/<id>/join`, and `/match/<id>/spectate`; it preserves the lobby connection while handing off to the existing player or read-only spectator WebSocket leg. The former `src/internal/live-runtime.tsx` query-driven entry (`?live&ws=&match=&name=[&token=]`) is historical/test-only compatibility for the direct-match E2E seam, not a production launch path.
    - `tests/e2e/full-stack.spec.ts`: real matchmaker creates a public 2p match, programmatic fill auto-starts it into the live server, TWO Chromium consoles join through the actual semantic runtime, ticks flow, orders fire from BOTH seats, authoritative acks correlate, per-seat fog-filtered views verified (own city visible, horizons differ, neither sees the whole board). Deterministic: all waits are poll conditions; fixed tick cadence. Host-wiring recipe documented in the spec's `buildStack` (server takes its bridge via `ServerDeps.matchmaker`; matchmaker hands handlers over via an optional `bindMatchmaker` on the server).
    - Review follow-ups closed: formatRejection table test (all 9 variants + confirmation messages; format.ts 54.5% → 95.45% stmts) and minimap real container sizing (`useContainerSize` ResizeObserver hook wired into App; component tests assert non-degenerate measurement + live resize).
    - Cross-feature findings fixed (no spec semantics changed): (1) adapter↔wire seq mismatch — `ConsoleClientImpl` assumed order-only numbering but the wire counter covers hello/join too, so every `orderAck` mis-correlated; adapter now consults the inner client's true `lastOrderSeq()` (fallback counter kept for legacy fakes). (2) Client heartbeat added after observing the server's FR-002 idle sweep close receive-only connections. (3) Terrain placement constraints require the default 32 board for matchmaking-generated matches (16×16 exhausts regeneration attempts) — E2E uses the shipped default.
    - Console totals at the integration-wave checkpoint: unit 227 · component 28 · a11y 19 · e2e 8 · perf 3 · determinism 3 · parity 2 · conformance 9 (= 299); merged coverage 89.72% stmts / 81.83% branches / 88.92% functions / 89.67% lines; bundle budget 80,791 B gz < 150 KB (the historical live-runtime chunk was lazy, 3.8 KB gz). client-ci.yml builds `@europa/matchmaking` before the Playwright step (E2E imports its runtime Node-side). Later semantic-routing work supersedes the checkpoint's direct live-runtime entry without invalidating these integration facts.
  - **History join** (`1150f2a`): merged the starter `main` into the branch with `--allow-unrelated-histories` (conflicts resolved ours), enabling PR #1.
  - **CI trigger scoping** (`2835e64`): all six workflows path-gated to their package + spec dirs + shared roots; push/pull_request branches = `[main]` only (no `001-europa-core` refs); live-validated on PR #1 (18 checks green).
  - **Host script** ("this commit"): `pnpm host` (`packages/console/scripts/host.ts`, node:* only) — one-command local match launcher mirroring the E2E `buildStack()` recipe: boots matchmaker ⇄ match server on `:8080` (`--port`/`HOST_PORT` override), serves built `dist/` on `:5173` via a ~40-line static handler with MIME map + traversal guard (`--static-port`/`HOST_STATIC_PORT`; fails with "run pnpm build" if dist missing), auto-creates+fills a public 2p match (32 board, 250 ms ticks) to trigger auto-start, prints two clickable join URLs. URLs carry per-seat session tokens so a refreshed tab reclaims its own seat within the grace window (tokenless first claim also works — wire join takes the first open seat). Bridge taps log seat joins + terminal result. Verified: headless two-seat smoke through the real wire (hello→join→seats 1&2→ticks→setReserves both seats→ok acks), SIGINT group-kill exits cleanly, EADDRINUSE → friendly message, engine 295 / matchmaking 170 / console e2e 8 unchanged.
  - **Waiting-for-opponent overlay** (`4dae654`, playtest-driven): a player who joins an unfilled match (1 of 2 seats) previously sat on a silent black grid — status "live" but no ticks until auto-start. Console now derives `isAwaitingMatchStart` (live + latestView null/tick 0) and shows a "Waiting for opponent to join…" overlay (live-region announced, reduced-motion honored, self-hides on first tick, never stacks with reconnect/game-over UI). Spec 005 Implementation Notes item 11; console suites now unit 234 · component 37 · a11y 23 · e2e 9 (+ waiting-overlay.spec.ts proving visible→hidden across real matchmaking fill).
  - **Live-wire defect fixes** (playtest-driven, 2026-08-24): two user-visible symptoms from `pnpm host` live matches — every board click crashing (`i.pipes.has is not a function`) and ticks freezing until refresh — traced to ONE root cause: the wire codec's Set→sorted-array transform (`frame.ts` wireReplacer) was never reversed in the browser client (Node fixtures rehydrated; the `/` demo supplies in-memory Sets; E2E never pointer-clicked against a wire-decoded view). Fix: `src/net/rehydrate-wire-views.ts` runs in `ws-match-client`'s onmessage before any consumer sees an envelope — one decode path covers live ticks, join snapshots AND the reconnect snapshot+replay window. Full sweep confirmed `CellView.pipes` is the only Set/Map-typed field in any server→client payload. The freeze chain: `buildMapView`'s render diff (`pipesEqual` → `.has`) threw inside App's `useMemo`, unmounting React while the socket stayed open. The PM's idle-sweep suspicion was DISPROVEN by code reading + tests: the client heartbeat is an app-level `ping` (schema-valid `{clientTimeMs}`), and the server counts every inbound frame as activity (`receivedSinceSweep` set pre-decode); both halves were already test-pinned. New guards: 5 helper unit tests + 3 client-path unit tests (incl. reconnect resync), 2 browser component tests over the FULL production chain (wire JSON → client → adapter → bridge → store → mounted App → real pointerdown; verified failing pre-fix), and `pnpm test:keepalive` integration suite (real sockets/real timers: quiet seat survives past 2× heartbeat with short config; negative control proves a non-pinging seat IS reaped with 1013) wired into client-ci.yml. Live proof on default-config host: two Node seats quiet 40 s (> 30 s idle window) with pong keepalives flowing throughout, then `ok:true` setReserves acks from both seats. Spec 005 Implementation Notes item 12 records the ruling (conformance repair against the existing ReadonlySet contract — no contract amendment). Console suites now unit 242 · component 39 · a11y 23 · e2e 9 · perf 3 · determinism 3 · parity 2 · conformance 9 · keepalive 2 (= 312); bundle 81,313 B gz < 150 KB.
  - **Issue #2 fix: 3-player auto-start GenerationError** (branch `issue-2-3-player-match`, 2026-08-25) — TWO coupled terrain defects: (1) INV-9 flagged the self-symmetric 3p middle player's legitimate same-owner mirror cities as asymmetry violations; (2) FR-004 point symmetry (fixed-point-free on even boards) × FR-005 equal counts made an odd `citiesPerPlayer` unsatisfiable for 3p — every attempt exhausted retries. Fix: INV-9 now compares the rotated partner's owner against exported `partnerPlayer(owner, playerCount)`; new `normalizedCitiesPerPlayer`/`normalizeSettingsForPlayerCount` in settings.ts rounds 3p `citiesPerPlayer` UP to even (1→2, 3→4), surfaced via `effectiveSettings`; generate.ts places self-symmetric players' seeds mirror-aware (center-first candidate order — periphery-first deadlocks on 33 boards); city-placement now enforces BOTH water metrics (validator Chebyshev ≥ min AND balance-oracle Manhattan > min) and takes `existing`/`mirrorAware` params for cross-band spacing. Spec 003 amended in-change: Clarifications v1.2, FR-005 inline note, data-model §2/§4/INV-7/INV-9, plan.md decision row. Tests: terrain 242 (new: city-count-parity unit, INV-9 3p cases, 60-seed×{32,33} 3p integration sweep incl. determinism + 2p/4p non-normalization), matchmaking 171 (new `matchmaker.autostart-3p.test.ts` — public 3p create→fill→auto-start through REAL generateBoard, proven failing pre-fix with the exact GenerationError). Engine 297 unchanged; repo typecheck/lint/format clean. PM-notable: placement acceptance is stricter than pre-fix (both water metrics), so generated 2p maps for a given seed may differ from earlier builds — still deterministic and valid; no committed golden board hashes exist (terrain/engine/console determinism fixtures are scripted or self-referential).
  - **009 shared app versioning: Implemented** (branch `009-shared-app-versioning`, T-001→T-012, 2026-08-25) — private zero-dep `@europa/version` exporting `APP_VERSION` + pure `checkVersionDrift()` + tsx CLI wired as root `pnpm version:check`; additive optional `HelloAckPayload.appVersion` (both contract copies byte-identical, networking conformance green, old-client tolerance pinned both directions); unauthenticated `GET /version` on the host static surface :5173 pre-SPA-fallback (`405` + `Allow: GET` on non-GET); HUD footer renders the bundled v-prefixed constant (#9ca3af on #111827 ≈ 6.99:1 AA), never a server hello value; README header + manual index footer lines are drift-check grep targets; first lockstep value **0.0.1** per Clarifications v1.1 — 0.1.0 deferred to release issue #4 bump-then-tag (**that bump lands with issue #4's change set on branch `issue-4-stabilization-release`**); dedicated path-gated `.github/workflows/version-drift.yml` on the issue-#3-hardened shape (workflow_dispatch live proof post-merge by design); companion specs amended same-change-set: 004 Clarifications v1.2, 005 FR-012 + ImplNotes item 15, 007 FR-017 + Clarifications v1.1. Gates re-verified this session: `pnpm version:check` exit 0, zero inline suppressions in all touched trees, `@europa/version` coverage 100%×4 (36/36 tests incl. all 10 spawned-CLI integration tests), console merged coverage 90.03% / 83.02% / 89.5% / 89.98% (stmts/branches/funcs/lines). Fresh recursive-suite counts: engine 297 · terrain 242 · fog 112 · networking 191 (+7 over its recorded 184 — feature-009 wire/logging suites) · matchmaking 171 · console node-mode 282 · version 36 = 1,331 repo total.
    - PM-notable: the first clean-slate gate run caught a missing `tsx` devDependency in `@europa/version` — a phantom stale binary had masked it during development; fixed in-branch (`11b27f6`) before completion. Lesson: final gates must run post-wipe.
  - **Release automation + v0.1.0 cut (this change set)** (branch `issue-4-stabilization-release`, 2026-08-25): `.github/workflows/release.yml` implements spec 009 FR-013 — fires on push to `main` when any FR-009 lockstep version surface changes (root/all workspace `package.json`, `packages/version/src/app-version.ts`; the workflow file deliberately NOT self-listed, else its own landing push would mint a spurious pre-bump release) plus guarded `workflow_dispatch` (typed `confirm_version` must equal canonical); extracts `APP_VERSION` from the constant file as single source of truth (fails loudly if missing/malformed); idempotent via remote tag check (graceful skip when `v<version>` exists, hard abort on unexpected ls-remote failure); mints tag + GitHub Release pinned to the triggering SHA with curated `release-notes/v<version>.md` notes when present, else generated; minimal `contents: write` + global non-cancelling concurrency group. Spec 009 amended same-change-set: FR-013, Out-of-Scope rewrite, Clarifications v1.2 (npm privacy permanent — binding decision 6; automation in scope via issue #4), Implementation Notes item 6. The v0.1.0 bump rides this same branch per FR-010's dedicated `chore(release): v0.1.0` bump-then-tag convention — squash merge gives main exactly one such commit touching every FR-009-guarded location, which triggers the first automated release.
- **Issue #30: elevation-based pipe flow rate + slope color-coding + terrain smoothing** (branch `issue-30-pipe-flow-rate`, 2026-08-30) — gradient flow model replacing the binary down/flat/up factors: `flowRateForDelta(delta, constants)` single pure formula in `@europa/engine` (asymmetric cap per PM ruling R-1 — downhill `flowBase + flowSlopeStep × min(|Δ|, flowSlopeDeltaCap)`, uphill `max(0, flowBase − flowSlopeStep × |Δ|)` uncapped, stall at Δ ≥ flowBase/flowSlopeStep); `EngineConstants` drops `flowDownhillFactor`/`flowUphillFactor`, gains `flowSlopeStep`/`flowSlopeDeltaCap` (both contract mirrors + quickstart Q-003 updated same change set; `ENGINE_API_VERSION` NOT bumped); ships `flowBase=7, flowSlopeStep=1, flowSlopeDeltaCap=5`. New configurable terrain setting `terrainSmoothing` (default 4, range [0,8] clamped, 0 = byte-identical pre-smoothing) — post-process 3×3 box-mean pass (divisor 9, `Math.floor((sum+4)/9)`, clamped coords) after symmetry enforcement, before water classification; surfaced via `effectiveSettings`; US4 AC-1 reachable-land floor ≥50% measured 54.53% (bidirectional flow-viability semantics). Console: `CellRenderInfo.pipeSlopes` (additive, both mirrors), `src/render/pipe-slope.ts` src-safe mirror + drift test pinning `PIPE_SLOPE_CONSTANTS` ≡ `ENGINE_CONSTANTS` over Δ∈{−10..10}; three-color scheme (downhill green/flat amber/uphill red) + hollow-triangle stalled treatment (shape change, not color alone); fog-unknown → flat. Design: 4 additive tokens reusing canonical values (pipeDownhill #059669, pipeFlat #f59e0b, pipeUphill #dc2626, pipeStalled #9ca3af), zero new hex literals, DESIGN.md §1.1/§3 + note N-4 (honest contrast: downhill 2.51:1 / uphill 1.96:1 vs darkest land tile < 3:1, ≥3:1 vs fog void; stalled has redundant hollow encoding) + spec 012 companion note. Matchmaking: ZERO src change (verified passthrough test only — settings flow via DEFAULT_GENERATION_SETTINGS, rematch carries over). Manual FR-012: pipes.md (gradient table 8–12/7/6→1/0), numbers.md (rows trace to ENGINE_CONSTANTS + terrainSmoothing row), index.md, the-board.md (elevation shading + smoothing subsection). Gates: engine 302 · terrain 366|50 skipped · fog 114 · networking 287 · matchmaking 404 · version 38 · console node-mode 605 (714 merged coverage) · browser component 92/a11y 29/e2e 17/perf 3/determinism 3/parity 2/keepalive 2/selfhost PASS (98,082 B gz < 153,600 B); coverage all ≥80% (engine 95.92/81.2/100/95.77, terrain 95.94/88.15/98.27/95.87, design 100×4, matchmaking 95.98/88.56/96.51/95.92, console merged 91.61/85.3/91.94/91.49); typecheck/lint/format/version:check/no-literals/vendor-identity/docs-privacy all green; console golden fixture genuinely exercised (no regeneration needed). Specs amended same-change-set: 001 Clarifications v1.1+v1.2 (FR-007 rewrite, US1 AC-4/AC-5, R-1 asymmetric-cap formula correction), 003 v1.3 (US4 + FR-010 + FR-008/SC-004), 005 v1.2 (FR-001 + FR-013), 007 v1.2+v1.3 (FR-006/FR-008/FR-010), 006 Implementation Note (additive). Orchestration log: `specs/001-core-game-engine/orchestration.md`.
- **Next** (tracked on GitHub — work each ticket in its own branch/worktree):
  - #2 `bug`: 3-player auto-start GenerationError (terrain package) — milestone v0.1.0 — **fix implemented on branch `issue-2-3-player-match`, pending PR/merge**
  - #3 `ci`: downstream workflows watch upstream package paths — milestone v0.1.0
  - #4 `chore`: cut v0.1.0 stabilization release (depends on #2 + #3) — milestone v0.1.0 — **in flight on branch `issue-4-stabilization-release`** (keep this line until merge)
  - #5 `enhancement`: Docker one-command self-host packaging
  - #6 `enhancement`: 3–4 player support end-to-end
  - #12 `chore`: migrate legacy feature specs to `specs/` — **done via branch `issue-12-specs-migration` (PR #19)**
  - #13 `docs`: root `pnpm host` script missing / README quickstart falls through to system host(1) — **done via branch `issue-13-host-script` (PR #20)**
  - #15 `ci`: hardening follow-ups from feature 009 — downstream watch of `packages/version/**` + job `timeout-minutes` — **in flight on branch `issue-15-ci-hardening`**
  - #25 `feat`: shareable design system `@europa/design` (single-source token table + deterministic `dist/design.css` + `europa-*` catalog, console + manual migration, living `DESIGN.md` contract, 4 cheap guards G-04/G-05/G-06 + workflow filters; full G-01..G-09 drift-suite + parity/smoke tests deferred by PO trim) — **implemented on branch `issue-25-design-system`, PR #31 open** (merging is the owner's call)
  - #35 `feat`: console semantic URL scheme — **Implemented (2026-08-31) on branch `issue-35-semantic-url-scheme` (Feature 013; semantic routing, Docker/runtime remediation, documentation, and final acceptance review complete)**
  - #30 `enhancement`: elevation-based pipe flow rate + slope color-coding (+ terrain smoothing pulled in by PO decision) — **implemented on branch `issue-30-pipe-flow-rate`, PR open** (merging is the owner's call)
  - #41 `feat`: shared UI web components in `@europa/design` — framework-agnostic `customElements.define` wrappers for generic + game-specific primitives — **spec written on branch `issue-41-shared-UI-components`, phases 1–3 complete** (Feature 014; spec at `specs/014-shared-ui-components/spec.md`)
  - Repo-wide test-typechecking gap remains a documented tradeoff (do NOT fix casually — every package's tsconfig excludes tests/ by design; CI compensates with dedicated strict programs). Deliberately NOT ticketed to avoid casual pickup.

- **Biome migration (Phase 1, 2026-08-24)**: the root configuration now layers
  the published `biome-config-shaunburdick@1.0.0` package over the existing
  repository style. It requires Biome `>=2.5.0 <3` and Node `>=22`; the root
  engine declaration and contributor instructions therefore use Node 22.
  Existing package configs remain layered children (`root: false`), and the
  console's three ARIA-grid accessibility exceptions remain package-scoped.
  adopted four-space/120-column formatting is enforced without mass-formatting
  the existing source. Full policy, exclusions, remediation order, and exit criteria
  live in `.specify/biome-migration.md`. Required checks are `pnpm typecheck`,
  `pnpm lint`, `pnpm format:check`, and targeted/full tests as appropriate.
  Status (2026-08-25): migration COMPLETE — per-package exception policies for all
  six packages are documented in `.specify/biome-migration.md` and `pnpm format:check`
  is clean repo-wide.
- Spec status lines: 001 = `**Status**: Implemented (2026-08-30)`; 003 = `**Status**: Implemented (2026-08-30)`; 002 = `**Status**: Implemented`; 004 = `**Status**: Implemented`; 006 = `**Status**: Implemented`; 005 = `**Status**: Implemented (2026-08-30)`; 007 = `**Status**: Implemented (2026-08-30)`; 009 = `**Status**: Implemented (2026-08-25)`; 012 = `**Status**: Implemented (2026-08-30)`
 - Spec status lines: 001 = `**Status**: Implemented (2026-08-30)`; 003 = `**Status**: Implemented (2026-08-30)`; 002 = `**Status**: Implemented`; 004 = `**Status**: Implemented`; 006 = `**Status**: Implemented`; 005 = `**Status**: Implemented (2026-08-30)`; 007 = `**Status**: Implemented (2026-08-30)`; 009 = `**Status**: Implemented (2026-08-25)`; 012 = `**Status**: Implemented (2026-08-30)`; 013 = `**Status**: Implemented (2026-08-31)`; 014 = `**Status**: Draft (2026-08-31)`; 015 = `**Status**: Draft (2026-09-01)`
- Monorepo test totals: more than 1,300 automated tests across the seven package suites; exact totals vary by package, project, and selected Vitest/Playwright configuration.

## Workflow rules

1. **Spec-driven development only.** No code without an approved spec; no implementation without plan + tasks. Follow the spec-kit phases; never skip.
2. **Git safety**: work on feature branches only; conventional commits (`feat:`, `fix:`, `docs:`, …); never push without explicit instruction; never rewrite history.
3. **Determinism discipline**: engine code must be pure (no wall-clock, no unseeded randomness, integer/fixed-point math); all tunable numbers live in one constants location.
4. **Specs stay truthful**: changing behavior means updating the spec in the same change set. Stale specs are bugs. This extends to player-facing docs (spec 007 FR-012): **any change set that alters gameplay behavior documented by the manual MUST update `docs/manual/` in the same change set** — the path-gated Pages workflow republishes it on merge. Stale manual numbers are bugs (the `docs/manual/numbers.md` appendix exists to be auditable against `ENGINE_CONSTANTS`).
5. **Licensing hygiene**: never copy code from `europa-source/` (SOS license, © Alex Nicolaou). It is reference material only — reimplement from documented behavior. The archive is a trimmed documentation subset (`html/Europa/` only); never modify files under `europa-source/`.
6. **Pre-push verification gate**: before pushing to a PR branch, run the **complete** project verification suite locally — not just the tests relevant to the changed package. This includes typecheck, lint, format, all package tests, browser-mode tests, E2E tests, and any guards. A failure discovered at CI time is a process failure.

## Environment notes (hard-won, verified this session)

- **Subagent reliability**: long-running subagent tasks in this environment may silently die ("stuck in thought", empty results, zero disk changes). Mitigations that work:
  - Chunk work into small single-artifact micro-tasks (one file per dispatch), verify each landing on disk before proceeding.
  - Give exact file paths (verify they exist first — a wrong path caused silent fast-fails).
  - Pre-create target directories before dispatching writers.
- If subagents keep failing, check `~/.config/opencode/agent/*.md` frontmatter: none pin a `model:`, so all inherit the session model. Pinning worker agents to a tool-call-reliable model fixes it (restart opencode after edits).
- `uvx --from git+https://github.com/github/spec-kit.git specify …` is the spec-kit CLI; current CLI uses `--integration opencode` (not `--ai`).
- **Light DOM child projection**: `<slot>` elements do not work in Light DOM — they are inert and do not project children. Components that need to wrap host children must manually reparent them via `appendChild` loops. The reparenting loop must run on every `render()` (outside the creation guard) to handle timing where `attributeChangedCallback` fires before children are appended (e.g., React 19 commits children after `connectedCallback`). This is the established pattern for all 20 web components in `@europa/design`.
- **Form-associated custom elements**: a custom element replacing a native `<button type="submit">` inside a `<form>` will NOT participate in HTML form submission — browsers only recognize native `<button>`, `<input type="submit">`, and `<input type="image">` as submit buttons. Use `static formAssociated = true` + `this.attachInternals()` in the constructor + `this._internals.form.requestSubmit()` in a click handler to restore form participation. `type="button"` elements with `onClick` handlers are unaffected. See `packages/design/src/components/generic/button.ts` for the reference implementation.
- **Test environment gotchas**:
  - **Vitest Browser Mode `setupFiles`**: run inside the browser, not Node.js. Node-only imports (e.g., `@axe-core/playwright`) in setup files poison the browser bundle and break all test files that transitively import them. Split browser-safe code into separate setup files.
  - **happy-dom gaps** (v20.x): `attachInternals()` not implemented (needs polyfill in `tests/setup-element-internals.ts`), `assignedNodes()` returns empty for light-DOM slots, `focus()` is a no-op on non-focusable elements (add `tabindex="-1"` to receive focus), `ResizeObserver`/`IntersectionObserver` absent.
  - **Playwright accessibility tree**: `getByRole('button')` does NOT traverse into custom elements to find internal native elements. Use `container.querySelector('europa-button button')` or similar DOM selectors for custom element internals.

## Restart procedure (fresh session)

1. `git branch --show-current` → expect a feature branch or `main` (post-MVP); read `git log --oneline -10`.
2. Read this file, the constitution, and skim all seven specs.
3. Determine phase: MVP complete and merged (`main`, PR #1 = `b1243cb`). Work queue lives in GitHub issues — milestone v0.1.0 first (#2 → #3 → #4), then #5/#6.
4. Continue from "Current state" above. When state changes, update the Current state section and commit.
