# Tasks: CI Workflow Hardening

- [x] T-001: Research latest action SHAs and audit current path triggers
- [x] T-002: Create `.github/dependabot.yml` for GitHub Actions ecosystem
- [x] T-003: Fix `matchmaking-ci.yml` — remove fog from paths, add workflow_dispatch, add concurrency, update SHAs
- [x] T-004: Update `engine-ci.yml` — add workflow_dispatch, add concurrency, update SHAs
- [x] T-005: Update `terrain-ci.yml` — add workflow_dispatch, add concurrency, update SHAs
- [x] T-006: Update `fog-ci.yml` — add workflow_dispatch, add concurrency, update SHAs
- [x] T-007: Update `network-ci.yml` — add workflow_dispatch, add concurrency, update SHAs
- [x] T-008: Update `client-ci.yml` — add workflow_dispatch, add concurrency, update SHAs
- [x] T-009: Validate all YAML files parse correctly
- [x] T-010: Verify no functional changes (same jobs, same steps, same commands)
