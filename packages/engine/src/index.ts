/**
 * Placeholder entry point for the @europa/engine package.
 *
 * Phase 1 (Setup) only bootstraps the build toolchain; no engine code exists
 * yet. This file exists solely so `tsc --noEmit` has at least one input and
 * `tsup` can produce a valid (empty) `dist/index.js` + `dist/index.d.ts`.
 *
 * Phase 2 task T017 (`.specify/features/001-core-game-engine/tasks.md`)
 * replaces this file with the real public surface re-exports.
 */
export {};
