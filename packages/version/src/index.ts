/**
 * Public surface of the `@europa/version` package.
 *
 * Deliberately tiny (plan §1): the single-source `APP_VERSION`
 * constant that every guarded surface projects.
 */
export { APP_VERSION } from './app-version';
