# Data Model: Player Manual (Feature 007)

**N/A — documentation feature.** No runtime entities, schemas, state machines, or API surfaces are introduced; the deliverables are Markdown prose and one GitHub Actions workflow file. The spec's "Key Entities" (`ManualPage`, `ControlReferenceTable`, `NumbersAppendix`, `PagesWorkflow`) are documentation constructs defined by FR-001..FR-016 in [spec.md](./spec.md), not software artifacts requiring a data model. Likewise `contracts/` is N/A: the binding "contract" for this feature is the spec's functional requirements themselves.
